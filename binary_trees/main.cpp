#include <iostream>
#include <cassert>

using namespace std;

// add function to insert node, root by value

// add function to insert node, root by reference

// add function to insert node, root by reference

// add function to print tree in ascending order 

// add function to print tree in descending order

// add function to find smallest value 

// add function to find largest value 

// add function to delete node


int main() {
  /* Node *root = NULL;

  root = insert(root, 20); // or just alternative_insert(root, 20)
  root = insert(root, 5);
  root = insert(root, 30);
  root = insert(root, 8);
  root = insert(root, 3);
  root = insert(root, 35);

  output(root);
  root = deleteNode(root, 20);
  cout << endl;
  output(root);
  cout << endl;

  output_descending(root);

  cout << endl;
 */
  return 0;
}